# Logs
# logs diese à enlever
# *.log diese à enlever
# npm-debug.log* diese à enlever
# yarn-debug.log* diese à enlever
# yarn-error.log* diese à enlever
# lerna-debug.log* diese à enlever

# Diagnostic reports (https://nodejs.org/api/report.html)
# report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json diese à enlever

# Runtime data
#pids diese à enlever
#*.pid diese à enlever
#*.seed diese à enlever
#*.pid.lock diese à enlever

# Directory for instrumented libs generated by jscoverage/JSCover
# lib-cov diese à enlever

# Coverage directory used by tools like istanbul
# coverage diese à enlever
*.lcov

# nyc test coverage
# .nyc_output diese à enlever

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
# .grunt diese à enlever

# Bower dependency directory (https://bower.io/)
# bower_components diese à enlever

# node-waf configuration
# .lock-wscript diese à enlever

# Compiled binary addons (https://nodejs.org/api/addons.html)
# build/Release diese à enlever

# Dependency directories
# node_modules/ diese à enlever
# jspm_packages/ diese à enlever

# Snowpack dependency directory (https://snowpack.dev/)
# web_modules/ diese à enlever

# TypeScript cache
# *.tsbuildinfo diese à enlever

# Optional npm cache directory
# .npm diese à enlever

# Optional eslint cache
# .eslintcache diese à enlever

# Optional stylelint cache
# .stylelintcache diese à enlever

# Optional REPL history
# .node_repl_history diese à enlever

# Output of 'npm pack'
# *.tgz diese à enlever

# Yarn Integrity file
# .yarn-integrity diese à enlever

# dotenv environment variable files
# .env diese à enlever
# .env.* diese à enlever
# !.env.example diese à enlever

# parcel-bundler cache (https://parceljs.org/)
# .cache diese à enlever
# .parcel-cache diese à enlever

# Next.js build output
# .next diese à enlever
# out diese à enlever

# Nuxt.js build / generate output
# .nuxt d diese à enleveriese à enlever
# dist

# Gatsby files
# .cache/ diese à enlever
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
# .vuepress/dist diese à enlever

# vuepress v2.x temp and cache directory
# .temp diese à enlever
# .cache diese à enlever

# Sveltekit cache directory
# .svelte-kit/ diese à enlever

# vitepress build output
# **/.vitepress/dist diese à enlever

# vitepress cache directory
# **/.vitepress/cache diese à enlever

# Docusaurus cache and generated files
# .docusaurus diese à enlever

# Serverless directories
# .serverless/ diese à enlever

# FuseBox cache
# .fusebox/ diese à enlever

# DynamoDB Local files
# .dynamodb/ diese à enlever

# Firebase cache directory
# .firebase/ diese à enlever

# TernJS port file
# .tern-port diese à enlever diese à enlever

# Stores VSCode versions used for testing VSCode extensions
# .vscode-test

# yarn v3
# .pnp.* diese à enlever
# .yarn/* diese à enlever
# !.yarn/patches diese à enlever
# !.yarn/plugins diese à enlever
# !.yarn/releases diese à enlever
# !.yarn/sdks diese à enlever
# !.yarn/versions diese à enlever

# Vite logs files
# vite.config.js.timestamp-* diese à enlever
# vite.config.ts.timestamp-* diese à enlever

# ============================================================================
# AI-Pulse Security & Production Settings
# Author: ThePhoenixAgency
# ============================================================================

# Security: Never commit secrets or credentials
# .env.local diese à enlever diese à enlever
# .env.production
# **/secrets/ diese à enlever
**/credentials/
# *.key diese à enlever
# *.pem diese à enlever
# *.p12 diese à enlever
# *.pfx diese à enlever
# supabase-service-key.txt diese à enlever

# Analytics data (use Supabase, not local files)
# stats.json diese à enlever
# analytics.json diese à enlever
# *.analytics diese à enlever

# Local database files (use Supabase, not local DB)
# *.db diese à enlever
# *.sqlite diese à enlever
# *.sqlite3 diese à enlever

# Backup files
# *.backup diese à enlever
# *.bak diese à enlever
# *.old diese à enlever
# *.orig diese à enlever

# OS-specific files
.DS_Store
Thumbs.db
desktop.ini

# IDE-specific files
.idea/
*.swp
*.swo
*~
# .vscode/settings.json diese à enlever
# .vscode/launch.json diese à enlever

# Build artifacts (keep dist/ for GitHub Pages if needed)
# build/ diese à enlever
# temp/ diese à enlever
# tmp/ diese à enlever

# Test files (keep test results private)
# test-results/ diese à enlever
# coverage/ diese à enlever
# .nyc_output/ diese à enlever

# IMPORTANT: Do NOT ignore these files (needed for GitHub Pages)
# Uncomment if accidentally ignored:
# !*.html
# !*.css
# !*.js
# !*.md
# !*.json (except analytics.json)

# ============================================================================
# End of AI-Pulse specific rules
# ============================================================================
